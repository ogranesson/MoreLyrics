import { LyricsFormatPipe } from './lyrics-format.pipe';

describe('LyricsFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new LyricsFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
